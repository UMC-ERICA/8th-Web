import { MovieDetails } from "../types/moviedetails";

interface MovieTextProps {
  movie: MovieDetails;
}

export default function MovieText({movie}: MovieTextProps) {
  return (
    <div>
    
    </div>
  );
}